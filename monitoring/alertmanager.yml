global:
  smtp_smarthost: 'localhost:587'
  smtp_from: 'alerts@expo-mcp.local'

route:
  group_by: ['alertname']
  group_wait: 10s
  group_interval: 10s
  repeat_interval: 1h
  receiver: 'web.hook'
  routes:
    - match:
        severity: critical
      receiver: 'critical-alerts'
      group_wait: 5s
      repeat_interval: 15m
    - match:
        severity: high
      receiver: 'high-alerts'
      group_wait: 10s
      repeat_interval: 30m
    - match:
        severity: medium
      receiver: 'medium-alerts'
      group_wait: 30s
      repeat_interval: 1h
    - match:
        severity: low
      receiver: 'low-alerts'
      group_wait: 1m
      repeat_interval: 4h

receivers:
  - name: 'web.hook'
    webhook_configs:
      - url: 'http://expo-mcp-server:3000/webhook/alerts'
        send_resolved: true
        http_config:
          basic_auth:
            username: 'alertmanager'
            password: 'webhook_secret'

  - name: 'critical-alerts'
    webhook_configs:
      - url: 'http://expo-mcp-server:3000/webhook/alerts/critical'
        send_resolved: true
        title: '🚨 CRITICAL: {{ .GroupLabels.alertname }}'
        text: |
          {{ range .Alerts }}
          Alert: {{ .Annotations.summary }}
          Description: {{ .Annotations.description }}
          Status: {{ .Status }}
          {{ end }}
    # Slack通知（実装例）
    # slack_configs:
    #   - api_url: 'YOUR_SLACK_WEBHOOK_URL'
    #     channel: '#alerts-critical'
    #     title: '🚨 CRITICAL Alert'
    #     text: |
    #       {{ range .Alerts }}
    #       *Alert:* {{ .Annotations.summary }}
    #       *Description:* {{ .Annotations.description }}
    #       *Severity:* {{ .Labels.severity }}
    #       *Status:* {{ .Status }}
    #       {{ end }}

  - name: 'high-alerts'
    webhook_configs:
      - url: 'http://expo-mcp-server:3000/webhook/alerts/high'
        send_resolved: true
        title: '⚠️ HIGH: {{ .GroupLabels.alertname }}'
    # slack_configs:
    #   - api_url: 'YOUR_SLACK_WEBHOOK_URL'
    #     channel: '#alerts-high'
    #     title: '⚠️ High Priority Alert'

  - name: 'medium-alerts'
    webhook_configs:
      - url: 'http://expo-mcp-server:3000/webhook/alerts/medium'
        send_resolved: true
        title: '⚡ MEDIUM: {{ .GroupLabels.alertname }}'
    # Email通知（実装例）
    email_configs:
      - to: 'devops@expo-mcp.local'
        subject: '⚡ Medium Alert: {{ .GroupLabels.alertname }}'
        body: |
          {{ range .Alerts }}
          Alert: {{ .Annotations.summary }}
          Description: {{ .Annotations.description }}
          {{ end }}

  - name: 'low-alerts'
    webhook_configs:
      - url: 'http://expo-mcp-server:3000/webhook/alerts/low'
        send_resolved: true
        title: 'ℹ️ LOW: {{ .GroupLabels.alertname }}'

inhibit_rules:
  - source_match:
      severity: 'critical'
    target_match:
      severity: 'high'
    equal: ['alertname', 'instance']
  - source_match:
      severity: 'high'
    target_match:
      severity: 'medium'
    equal: ['alertname', 'instance']
  - source_match:
      severity: 'medium'
    target_match:
      severity: 'low'
    equal: ['alertname', 'instance'] 